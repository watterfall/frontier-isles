# 十岛考察发布记录 · 2026-09-08

岛屿建筑细节与十岛考察已部署到 [Frontier Isles](https://frontier-isles.fly.dev/#island=compositional-modeling)。进入实验坊的“自由考察”，可以改变条件、观察计算结果，带着问题进入对应文献房间，再返回继续；条件与个人追问按岛保存在浏览器本地。手机端也能从十岛索引和下一岛线索继续探索。

## 发布身份

| 项目 | 证据 |
| --- | --- |
| 运行提交 | `dafe39543a5f421442c95b7542575178e5bd3732`，已推送 `main` |
| 运行 CI | [34177384037](https://github.com/watterfall/frontier-isles/actions/runs/34177384037)，成功后部署 |
| Fly 发布 | **v9**，2026-09-08 01:59:25 UTC；产品迭代名为 v7，两者编号不同 |
| 镜像标签 | `registry.fly.io/frontier-isles:git-dafe39543a5f421442c95b7542575178e5bd3732` |
| 镜像摘要 | `sha256:5ff2dcd464b1eda7161b5d7eadd5a570a551e11cd8cf52108b1c0316230715e5` |
| 部署方式 | 先远程构建并推送提交专属镜像，CI 成功后更新既有 nrt 单机，保留 SQLite 卷 |

本记录和发布清单由后续纯文档提交补入，不在上述运行镜像中。[发布清单](release-manifest.json)中的 `main.commit` 指运行检查点，不自动代表以后每个 `main` HEAD 都已部署。

## 内容与交互

- 建筑加入与用途相关的细节；地图、建筑列表与场景共用预览，键盘定位、读过的房间提示、返回岛屿和手机视野保持连续。见[建筑与导航实施记录](island-experience-v6-2026-09-08.md)。
- 十岛分别围绕形式化数学、因果表征、组合式建模、AI 理论发现、自学习物理网络、最小细胞、动物交流、量子擦除、超均匀性和细胞扰动评测展开。每岛有自己的操作规则、计算图示、来源与适用边界。见[选题、原始来源与详细设计](island-field-studies-v7-2026-09-08.md)。
- 实验坊与文献阁使用真实房间地址；札记显式追加并保护已有文字，存储不可用有明确反馈。探索线索属于编辑提出的问题迁移，不自动生成科研证据或关系边。

## 发布前修复与数据保留

生产旧目录仍把 `universal-ml-interatomic-potentials` 指向 XF-1422，当前目录已合并到 XF-1389。原严格身份校验会阻止已有数据库启动。此次仅允许这一明确的 slug / 旧编号 / 新编号组合；其他身份冲突仍拒绝，包括反向迁移、其他旧编号和其他岛屿。

新增回归覆盖元数据更新、原文与账本保留、重复运行无变化和三类拒绝条件。部署前后 API 对照确认 `opId`、slug、问题对象、正文、Markdown、建筑、成员关系、事件数量不变，账本字节一致，SHA-256 为 `4d630ba936e263b09eec04c98c1a761960f936e7c620eaa98fb9578b84654b87`。

部署前通过 SQLite backup API 完成一致性备份 `/data/isles-pre-v7-20260908.db`（7,991,296 字节，384 个岛屿），保留在同一生产卷。Fly 卷快照请求仅返回“已安排”，没有确认完成，因此不将其作为已完成的独立备份。若要回退旧运行版本，需要先评估上线后写入并处理其对旧目录编号的要求，不能仅切换旧镜像而假定数据库兼容。

两处既有浏览器检查也得到修正：跨域碰撞入口等待实际异步对话框出现；地图相机的就绪、恢复与状态捕获在同一浏览器任务中完成，避免读取已被替换的渲染器。

## 验证结果

| 层次 | 结果与范围 |
| --- | --- |
| 本地完整验证 | 最终 `pnpm verify` 成功：1,253 项工作区测试 + 22 项根脚本测试，类型、数据投影、导入检查与生产构建通过；包含 435 项前端与 154 项服务端测试 |
| 本地完整浏览器 | 独立服务与新 SQLite 数据库，27/27 通过，无重试 |
| 运行提交 CI | test / typecheck / build / E2E 均成功；26 项直接通过、1 项重试通过、0 失败。重试项是既有模拟 AI 提案流程等待“找一个能反驳它的解释”超时，不能表述为 27 项均首次通过 |
| 线上 API 与资源 | health 成功；384 岛、126 结构、135 映射、39 前沿投影。入口 JS/CSS、延迟加载的 FieldStudy 与 GeneratedIslandScreen 资源字节均与本地验证构建一致，完整散列在发布清单中 |
| 线上浏览器 | 对正式域名运行 `apps/web/e2e/field-studies.spec.ts`，5/5 通过、无重试，55.7 秒。覆盖全部十种考察条件与观察、原始来源链接、来源房间返回、草稿刷新与札记、满额和不可用存储、390px 英文键盘交互与跨岛导航、无 WebGL 建筑房间入口 |
| 线上可访问性范围 | 桌面与手机视口的考察区域 Axe 无报告项；不是全站认证，也未在实体手机上验收 |

入口 JS 为 870.77 kB raw / 289.24 kB gzip；入口 CSS 为 240.06 / 36.24 kB；按需 FieldStudy 为 23.94 / 10.92 kB。使用十进制 kB、gzip level 9；既有入口 1024 KiB 与 CSS 244 KiB 预算不变。大型延迟分包的构建警告仍存在。

线上验证的本地日志为 `/tmp/frontier-v7-live-e2e.log`、`/tmp/frontier-v7-release-live.json`；CI、构建和部署日志分别为 `/tmp/frontier-v7-ci-complete.log`、`/tmp/frontier-v7-fly-build.log`、`/tmp/frontier-v7-fly-deploy.log`。这些临时日志不属于版本化发布证据，持久摘要与散列以本记录、发布清单及 GitHub CI 为准。

## 保留的边界

线上 xFrontier 为实时数据集 `xf-4ab72b2e670e`，55 个问题，已核对 `GQ-life-origin` 的 DOI 来源。GitHub OAuth 仍返回 501；AI 助手报告 `unconfigured`，未验证真实模型调用或已认证的生产账本写入。浏览器验收只在临时浏览器本地写入私人草稿和札记。

Fly 继续使用空闲停机配置，首个请求可能经历冷启动。以上为实现、部署与限定交互验证；教学模型的运行、文献链接和设计审查不构成科研结果或真人可用性研究。
