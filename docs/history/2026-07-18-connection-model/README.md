# Connection and executable-model implementation archive

These files preserve the execution trail that closed the 2026-07-18 connection, dossier, language, and first executable-model slices. They are retained for provenance and forensic verification, not as current agent instructions.

- `task_plan.md` records phased acceptance criteria and completed checklists.
- `findings.md` records discoveries, decisions, source notes, and browser observations.
- `progress.md` records implementation and verification events in chronological order.

Start from `docs/PROJECT-CORE.md`. Use `docs/ROADMAP.md` for current planning, then open one of these records only when a present claim needs historical evidence. “Next” or “current” language inside the archived files describes the session in which it was written.
