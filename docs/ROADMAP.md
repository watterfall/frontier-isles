# Frontier Isles · Roadmap Assessment

_As of 2026-07-16, after Phase B closed (§8 accepted), the richness + world-map campaigns, the vertical/nested-world passes, and the current continuous-world exploration slice. V1 nested world **landed** (`548fa26`); the frontier atlas grew 26→78→128 islands and received the comfortable-at-every-tier and world-tier legibility passes. The current working tree adds a navigable L0.5 field layer between atlas and island: real continuous flight, question-led courses, altitude alignment, current sampling, survey notes, docking, and pose-preserving return. It now also turns those traces into a versioned local-first field notebook: reload-safe pose/course/survey/current/note records, a visible survey log, and portable Markdown export. Companion to `architecture.md` v3.0 §8, `depth-plan-v1.md`, `depth-plan-v2.md`, `atlas-world-plan.md`, and `nested-vertical-world-v1-2026-07-11.md`; this file records **actual** status and the prioritized path forward. The 2026-07-16 closeout adds a full-stack Playwright smoke for the L0→L0.5→L1→L0.5→reload loop, refreshes the archipelago snapshot to the current causal-science cluster name, and serializes Pixi app lifetimes so StrictMode cannot corrupt the shared text-texture pool. The follow-up data-boundary pass generates a browser-safe L0 projection, moves deep station archives behind an L1-only dynamic import, and lazy-loads the generated-island screen: the main production chunk falls from 2,649.66 kB / 954.23 kB gzip to 811.84 kB / 263.87 kB gzip. Verified green: 645/645 Vitest tests, all workspace typechecks, production build, and 1/1 Chromium E2E._

## 1 · Status vs milestones

| Milestone | Status | Evidence / gap |
|---|---|---|
| **P0 Protocol** | ✅ **Done** | `packages/opp`: hand-written `.md` validates and parses into all typed references; hash-chained ledger with tamper/reorder detection; projections aggregate only from events. |
| **P1 Skeleton** | ◐ **~90%** | Renderer + PixiJS engine ✅ · asset extraction ✅ · founding ceremony writes object + genesis events, island rises and persists ✅ · GitHub OAuth (state-bound, logout, session UI) ✅ pending real creds · Question Wall L2 ✅ · list twin ✅ · Yjs presence ✅ · **scene generator → founded/curated islands are enterable** ✅ (Track B self-build). **Single remaining gap:** whiteboard co-editing has server rooms + ledger fold but **no canvas UI** — the "two people co-edit" acceptance isn't demonstrable yet (**consciously deferred 2026-07-12** pending a positioning discussion — §3.1; deploy + presence names are the actionable P1 items meanwhile). |
| **P2 Residents** | ✅ **Done 2026-07-11** (§4 Phase B — the §8 chain accepted live; residual caveats stay in §3) | The old #1 debt — no L1 scene for founded islands — is **closed**, and L1 now renders in the layered PixiJS engine from the real ledger, with day/night, sea-as-data, event-driven claim structures, and SVG no-GPU fallback. CrossRef scout → night shift → morning report → dock-mediated transplant → evidence-gated claim → publication ran live with a valid chain; night-digest webhooks and human-only transplant are shipped. Relations remain available as data/projections but the always-on homepage layer is hidden pending the lens rework (§3.10). **Residual caveats:** hero SVG lamps remain partly decorative; CJK-only questions weaken CrossRef recall; gateway validate references still need projection-side ref resolution; degraded dock-proposal digests currently also trigger webhooks; whirlpool is projected but not rendered. |
| **P3 Atlas** | ◐ **~78%** (default 4-layer world + vertical nesting + continuous field exploration + local notebook live in the current working tree) | The default L0 **is** the Pixi semantic-zoom world map (`atlas-world-plan.md`): climate continents → balanced named regions → islands, one continuous camera, fog-as-focus, real search, scale-tested at N=700; SVG remains the no-GPU fallback. The vertical/nested passes add cartographic `altitudeZ`, anchor/satellite disclosure, real-current air routes, and mobile hierarchy. **L0.5 now makes the atlas traversable:** a craft can set a question course, steer continuously, align altitude, survey an island, sample currents, dock into L1, and return to the recorded berth. The local-first notebook restores those records on reload and exports them as Markdown. **My Harbor landed 2026-07-12. Gaps:** account/ledger notebook sync (intentionally not faked with an unrelated OPP action); relations lens (§3.10); ferryman economy; ORCID / DOI export / Merkle anchoring. Leavability (`.md` notebook + existing `ledger.jsonl` export) is live. |
| **P4 Federation** | ✗ Not started | — |

**Cross-cutting invariants held:** no XP/leaderboards; list twin everywhere; AI never pushes (gateway degrades to dock proposal — e2e-verified); place events in a separate stream; dissolved-never-deleted; bilingual UI (key-parity test); **the sea is data too** (invariant 14 — every current/climate band transcribes a ledger relation or domain vector; there is no "draw a link" tool); **altitude is cartographic position, never ranking** and **anchors are navigation entries, never value** (vertical-archipelago / nested-world contract — tested: navigation roles do not change when activity changes).

## 2 · What shipped since the 2026-07-07 assessment

1. **Scene generator** (`scene/generator.ts` + `GeneratedScene.tsx`, 6 tests) — founded and curated islands now render a full L1 scene from the place plane + growth stage. This closes the single highest-leverage gap; Track B ("self-build" founded islands) is enterable.
2. **`packages/data`** — a curated xfrontier atlas of 26 real frontier islands (`frontiers.ts`) plus `bridges.ts` and `sea.ts`; 27 unlocked on the chart, i.e. 26 + the sample (was: sample only).
3. **v2 「海即数据」 sea-plane** — the horizontal sea becomes a live data field, not backdrop. `core/currents.ts` (`projectCurrents` / `projectWhirlpools`), `renderer/sea.ts`, `assets/sea/*` (Current, ClimateField, SeaDepth, FlowLegend, RelationsList) and `web/SeaLayer.tsx` render currents + climate depth + flow legend + focus salience live from the ledger. Adds invariants 14–16 (`depth-plan-v2.md`). **Update:** these sea-plane visuals are now hidden on the homepage pending a relations-representation rework — code + tests retained, not rolled back (§3.10).
4. **`.design-eng-loop/`** — an adversarial multi-round design↔code co-evolution contract (dual-axis engineering + taste rubric, maximin floor-raising, 5 rounds logged in `coevo.jsonl`); `design-system/` holds 22 foundation + v2 dimension cards.
5. Bilingual sample-island editorial content, richer hover cards, de-abstracted UI.
6. **Phase B closed end-to-end (2026-07-10/11)** — real AI resident (CrossRef scout) → ledger-driven night replay → transplant-through-dock → evidence enforcement + steles → night-digest webhook → the full §8 chain accepted live on machine-curiosity (details in §4 Phase B).
7. **depth-plan-v1 reconstructed + richness campaign (2026-07-10)** — `depth-plan-v1.md` (vertical world, ferryman, My Harbor, drift bottle, terrain fingerprint, rituals; invariants 10–13 + 17) written to disk at last (`317f8ab`; plan: `richness-plan.md`); L1 moved to the layered Pixi scene engine with night replay.
8. **World-map campaign (2026-07-11, plan `5f9307d` → landed `17f5aa2`, 6 lanes W1–W6)** — the Pixi semantic-zoom atlas promoted to **default L0** (lazy Pixi + WebGL detect + SVG fallback); T0 = 4 named climate continents (`projectClimate`) + currents + fog; T1 = hybrid-named regions with **k-means++ replacing single-linkage** (700 islands: max cluster 4.3%, 0 numbered fragments, 34ms); `makeScaleCorpus` honest synthetic corpus for N=200/700; continuous drill-down + fog-as-focus + richer T2 (tide glow, members, resolved lighthouse, dormancy fade).
9. **World-exploration + UI/interaction flagship passes (2026-07-11)** — L0 islands carry data-driven buildings/vegetation/resident marks/smoke; real bilingual search (`/` focuses, result → camera flight → ferry → L1); viewport-filling shell (no more ~800×500 letterbox at 1280px); unified L1 dossier headers; Day/Night as a labelled `button[aria-pressed]`; mobile swapped its hard-coded mock for the real reconciled island list with 44px targets (`world-exploration-design-pass-2026-07-11.md`, `ui-interaction-pass-2026-07-11.md`).
10. **Vertical archipelago → V1 nested vertical world (2026-07-11, `2948013` + working tree)** — L0 raised into orthographic pseudo-3D: altitude bands folded from place-plane N–S (**height is cartographic position, never ranking** — the legend says so), rock-strata island bases, air shadows, wind lines; cross-domain currents lifted into elevated air routes; fog cells → two cloud bands; altitude filter on desktop + mobile. The V1 pass (working tree, contract `nested-vertical-world-v1-2026-07-11.md`) adds continuous `altitudeZ`, anchor/satellite nesting (`assignAtlasHierarchy` spatial medoid = navigation anchor; `satelliteReveal` mid→near disclosure with hit-test gating; first click flies in, second click enters), mobile `buildMobileHierarchy` (27 → ≤8 anchors, tap-to-disclose, roles never track activity), and the bilingual hierarchy legend.

## 3 · Known debt (honest list)

1. **Whiteboard canvas UI missing** — server rooms + fold-on-empty done (the `island:<slug>:canvas` Yjs room already folds its `strokes` array into a `canvas_snapshot` → `bridge_artifact` event on empty; §5 makes this the canonical intra-artifact CRDT surface); no client drawing surface, so it is technically the single P1 acceptance blocker. **Deliberately deferred (2026-07-12): important, but pushed back on purpose pending a dedicated positioning/design discussion for the station** (deferred by decision, not oversight). Existing positioning stands as documented in `architecture.md` §5 (九站: Whiteboard Hall 白板厅); the near-term Phase A work is deploy + presence names, not this.
2. **Sea-plane only partly rendered** — currents / climate / depth / legend / focus are live; whirlpool is projected + API-served but not drawn; strait/isthmus, confluence/estuary, trade wind, and archipelago clustering are specified in `depth-plan-v2.md` + design-system cards but not yet built as components.
3. Night replay ghosts/thresholds partly hardcoded to the seeded story; should render purely from `GET /events?upTo`.
4. L1 buildings don't yet reflect growth stage/level (the chart does).
5. Claims/evidence linkage unenforced; `attach_data`/`attach_hardware`/`create_driftwood` map onto `night_digest` (no native ActionType — an `opp/0.3` addition would be a breaking protocol change needing a change note).
6. Literature scout not connected to CrossRef (no real AI resident feeding the night shift yet).
7. Production hardening: deploy (single process), session expiry/cleanup, rate limits, `secure` cookies behind TLS.
8. **Bundle regression — initial-load portion closed 2026-07-16.** The web now imports direct `@frontier-isles/data/*` subpaths; generated `atlas.ts` contains only the L0 projection and is drift-checked during data-package typecheck; `GeneratedIslandScreen` is a 39.37 kB async chunk; and the 1,210.00 kB / 482.99 kB-gzip interior archive loads only when an L1 server response is missing its interior. Main: 2,649.66→811.84 kB (**−69.4%**); gzip: 954.23→263.87 kB (**−72.3%**). **Residual:** the rare stale-server fallback archive is still one large chunk and triggers Vite's >500 kB warning; split it by interior batch or slug only if real fallback telemetry shows it matters. In-SVG captions remain zh in EN mode (deliberate, documented).
9. Drizzle deferred (plain SQL); revisit when schema churn starts.
10. **Relations rework — deferred, not dropped.** The sea-plane relation visuals (currents / whirlpools / straits / isthmus / hardcoded bridges / lineage line / flow legend / the 关系海 RelationsList sidebar), plus the domain-category UI and several numeric readouts, are now **hidden on the homepage**. The code (`apps/web/src/components/chart/SeaLayer.tsx`, `packages/core/src/currents.ts`, `packages/renderer/src/sea.ts`, `packages/assets/src/sea/*`) and its tests are **retained** — a rework-later decision, not a rollback. Reason: always-on, the relation layer read as cluttered/occluding on the chart, and the relation model itself lacks a good representation + interaction plan. Rework must find a better form — candidates: an on-demand **relations lens** (a toggle, not always-on), LOD-gated currents, focus-only reveal, or the `depth-plan-v2.md` taxonomy rendered more legibly. Tracked as a P2/P3 item (Phase C). **First reworked form shipped 2026-07-11:** the vertical atlas lifts real cross-domain currents into elevated **air routes**, LOD-gated to mid/near zoom instead of always-on — the relations-lens decision should build on this, not resurrect the always-on SeaLayer.
11. **Canvas scaling — largely closed 2026-07-11.** The default L0 is now the Pixi semantic-zoom atlas (continuous camera, 3-tier LOD + cross-fade, billboard label deconfliction, k-means++ balanced regions, fog-as-focus, vertical nesting; N=700 at 1–1.6ms renderMs in SwiftShader); the SVG chart survives only as the no-GPU fallback. `INFO-HIERARCHY.md` remains the governing principle (info shown = f(zoom, density, variance-select) at every scale). **Still open:** real-device retina perf at N=700 unverified; region washes blur into each other at high density; currents not yet joined into the clustering distance; cluster/region labels still outside the *unified* label-deconfliction pass. **2026-07-12 world-tier legibility pass (`9a50eb4`):** domain names get a feathered paper halo + a firmer outward push so region chips read as resting *on* a watermark instead of slicing live text; far-tier routes recede (a lone arc no longer reads as a stray stroke); region chips carry a domain-ink spine; and `ATLAS_Y_SPREAD` reframes the flat 2.45:1 island strip toward the viewport aspect (fit re-centres for free; the 78 authored coords untouched). The wash-separation and unified-deconfliction items above still remain.
12. **Richness v1 — partially landed, one semantic conflict to resolve.** `depth-plan-v1.md` is now reconstructed and canonical (`317f8ab`); the ~8s transplant walk shipped (lane E, fires on the real event); My Harbor / fog shipped **end-to-end 2026-07-12** (server footprint endpoint → open-at-harbor camera + overview fog dim + 回港 control; was projections-only). **Still unbuilt:** ritual moments (river lantern 河灯 on publish / storm on dispute / rainbow on resolution), drift bottle 漂流瓶, resident tree 居民之树, citation birds 候鸟, terrain-fingerprint procgen. **New tension (needs §5 decision 7):** the vertical-archipelago contract deliberately spends the vertical axis on *cartography* (`altitudeZ` = folded place-plane position, explicitly not meaning-bearing), while `depth-plan-v1.md` §1 wanted sky/sea/seabed = *epistemic groundedness*. Both cannot own island altitude; reconcile before v1 ratification.
13. **L2 depth panels** still use inline styles and clickable `div`/`span` surfaces — they need the same semantic-control + focus-state treatment L0/L1 received in the UI pass (`ui-interaction-pass-2026-07-11.md` debt 1).
14. **Exploration durability — local-first layer closed 2026-07-16; account sync open.** Visited islands, sampled currents, course history, craft pose, and personal observations now persist through a versioned, defensively parsed browser notebook; reload starts safely at L0, re-entering L0.5 restores the field position, and the same record exports as portable Markdown. It is explicitly labelled local-only. Server/account/ledger sync remains open because OPP has no semantically correct field-observation action; do not overload `validate`, `transplant`, or `night_digest` merely to claim ledger persistence.
15. **Accessibility debt in the deep UI.** L2 still has non-semantic click targets and heavy inline styling; small labels/touch targets remain, and the ochre/pale-ink palette contains text combinations below WCAG AA contrast. Treat this as a focused semantic-control + token pass, not scattered one-off fixes.

## 4 · Prioritized plan

### Phase A — close P1 (small; days)
_Immediately-actionable P1 work is **deploy** + **presence names**. The whiteboard, though it's what formally completes the P1 acceptance, is **consciously deferred** (2026-07-12) pending a dedicated positioning/design discussion — see §3.1._
- **Deploy** single process (Fly.io per §6.1) — **runbook: `docs/deploy-fly.md`**. Single-process web serving + `Dockerfile`/`fly.toml` landed and container-validated end-to-end (2026-07-12, `1d4f303`); only the operator credential steps remain (flyctl auth, `fly apps create`, GitHub OAuth app + secrets, DB volume, `fly deploy`). CI (`pnpm -r test && typecheck && build`) still to add; the local full-stack `pnpm test:e2e` world-exploration smoke now exists and should be wired into that gate.
- Presence names/positions in awareness (not just a count).
- **Whiteboard canvas UI** (⏸ **deferred 2026-07-12**, awaiting a positioning discussion) on the existing `island:<slug>:canvas` room (fold-on-empty already server-side; server half ready, client-only build) → completes the P1 acceptance: two browsers, see each other, co-edit, replay from the ledger.

### Phase B — close the P2 loop (the product's proof; weeks)
1. **First real AI resident**: literature scout via CrossRef → `create_driftwood` through the MCP gateway → night-shift lamps from real events → morning report drafted from last night's ledger (not seeds).
   **✅ B.1 landed 2026-07-10** (`90a7217` scout · `da87ef7` morning report · `9914d34` seam): `apps/scout` runs a real CrossRef night shift through the MCP stdio gateway as `github:curiosity-scout`; morning report is projected from the ledger (`core.projectMorningReport`) with adopt/return writing real `adopt`/`return_to_driftwood` events; live-verified end-to-end (shift → draft → UI adopt → ledger, chain intact). *Caveats:* generated-island Pixi lamps are event-driven, but the hero island's SVG scene still lacks per-station lamps; CJK-only QFocus weakens CrossRef recall; adopt hardcodes `credit:ai/synthesis` regardless of the draft's AI credit role.
2. Ledger-driven night replay end-to-end (ghosts typed from events; timeline = event index).
   **✅ B.2 landed 2026-07-10** (`21f5cfd` core contract · `ca4c685` Pixi replay · `3f4e116` hero reveals): `core.projectNightTimeline` (nights axis, notable-event markers, scrub→upTo mapping, clock-free); generated islands scrub the real ledger — claims/ghosts/active-station lamps rebuild per night (rAF-throttled); hero island's scrubber + ghost reveal tiers come from its real ledger (today's `adopt` is the newest marker), seed constants demoted to the offline fallback. *Caveats:* hero night lanterns/fireflies/hint chips are still `night`-boolean stage dressing, not event-bound; `return_to_driftwood` is not in `TIMELINE_MARKER_ACTIONS` (scene layer reuses it for ghost tiers without widening the core contract).
3. Transplant-through-dock: transplant produces one of the four bridge artifacts at the dock, then places it at the target station.
   **✅ B.3 landed 2026-07-11** (`7a03bae`): `core.buildTransplant` + POST `/api/islands/:slug/transplant` (station_write role check; MCP deliberately has NO transplant tool — human-only per §4); the bridge-artifact ref carries a permanent `onceDriftwood` mark; dock + target-station placements; TransplantPanel in the live L1; the lane-E ~8s transplant walk fires on the real event. Live-verified: the scout's curiosity-model paper was transplanted driftwood→dock→library as a hypothesis-formalization, `onceDriftwood` pointing at the exact source ref.
4. Claims/evidence enforcement + steles before the Gallery.
   **✅ B.4 landed 2026-07-11** (`a26d0c8` merge): refute/validate without an evidence-role data ref are rejected at the gateway (HTTP 422 `evidence_required` / MCP tool rejection, checked BEFORE capability degrade — record honesty, not permissions; write-side strict, projection-side tolerant, no ledger migration); claim towers are now steles arced before the Gallery (base=foundation, inscription rows=replications, canopy+halo=consensus, ghost steles night-only). *Caveat:* gateway-written validates ref the response content, not the claim ref, so projectClaimState misses them for floors (pre-existing gap, needs ref resolution in the projection).
5. Night-digest webhook (Slack / Matrix / Feishu).
   **✅ B.5 landed 2026-07-11** (`3d96e40`): `NIGHT_DIGEST_WEBHOOKS` env (comma-separated; provider sniffed per URL — Slack / Feishu / generic-JSON for Matrix hookshot); fire-and-forget after `gateway()` writes a `night_digest`, never blocks or fails the write, retry-once; seed replay deliberately silent; only night_digest is ever pushed (the platform is not an IM). *Caveat:* degraded dock-proposal night_digests also push (no effectiveAction filter yet).
6. **Acceptance (§8):** one island runs AI night shift → morning report → bridge → transplant → claim → publication, every step ledger-traceable.
   **✅ §8 ACCEPTED 2026-07-11.** The full chain ran live on machine-curiosity (32 events, `opp.verifyChain` → ok), every link a ref-equality check:
   ① AI night shift — 11 `night_digest` events by `github:curiosity-scout` (real CrossRef finds) → ② morning report `adopt` (human+AI pair, `82697ba0…`) → ③ `transplant` through the dock — `hypothesis-formalization` → library, `onceDriftwood` = the scout's exact driftwood ref (`4a5f24bc…`) → ④ `submit_claim` whose content `basedOn` = the bridge-artifact ref → ⑤ `validate` with a replication-role evidence RO-Crate, referencing the claim ref (evidence enforcement passed it; the no-evidence control was 422-rejected) → ⑥ `publish` whose `claimRef` = the claim ref. Phase B is closed; remaining caveats live under items 1–5 above.

### Phase C — P3 atlas (relations rework + canvas scaling + interop)

**◐ C1+C2+C3-data landed 2026-07-11** (`75d4b40` atlas stage · merge of archipelago/harbor projections · `e987747` integration): the L0 Pixi atlas (`renderer/pixi/atlas-stage.ts` + `atlas-lod.ts`, behind `?atlas=pixi`, SVG ChartScreen still the default) has the pointer-anchored zoom/pan camera, 3-tier semantic LOD with cross-fade, spatial-hash billboard label deconfliction (re-layout on camera settle only), and variance-select outlier glow; `core.projectArchipelagos` (deterministic single-linkage over spatial × domain-vector × current-strength; statistical outliers never clustered; derived bilingual names) + `core.projectHarbor`/`fogLevel` are live projections, and the demo's far tier renders the REAL clustering — 26 curated islands → 5 named archipelagos + 1 floating outlier; 727 → 20 in ~140ms, mid-tier renderMs 1–1.6ms (SwiftShader). INFO-HIERARCHY §6 acceptance: dot-sea ✓ (≤20 named regions), find-in-≤3-zooms ✓, island-label deconfliction ✓. **Remaining for Phase C:** superseded — see the consolidated C4 list below (the "promote to default L0" item was done 2026-07-11 by world-map lane W1). *Honest findings recorded in code:* "variance-select math in `renderer/sea.ts`" was a doc myth (it's a string label — the statistical detection was written fresh in `archipelago.ts`); there is no cross-island membership API yet, so My Harbor reduces over the caller's accumulated events/footprint.
**◐ C4 · World-map campaign + vertical nesting landed 2026-07-11** (§2 items 8–10; plan docs `atlas-world-plan.md` → contract `nested-vertical-world-v1-2026-07-11.md`): the semantic-zoom atlas **is** the default L0; climate continents + hybrid-named regions rendered; k-means++ balanced clustering at 700; continuous drill-down into L1; real search → fly-to; vertical archipelago (altitude bands, air routes, cloud bands, altitude filter) committed at `2948013`; V1 nested world (continuous `altitudeZ`, anchor/satellite disclosure, mobile hierarchy) landed in `548fa26`.

**Phase C — prioritized remainder (the current plan):**
1. **Close the continuous-world slice** — ✅ current working tree: refresh the causal-science archipelago snapshot; add the full-stack browser smoke for atlas → course → real flight → survey → dock → L1 → restored berth; stop residual velocity when a survey completes. Before commit, keep `pnpm test`, `pnpm typecheck`, `pnpm build`, and `pnpm test:e2e` green.
2. **Turn the field note into a durable research notebook** — ✅ current working tree: versioned local-browser persistence for pose, surveyed islands, sampled currents, course history, and personal notes; safe L0 boot + restored L0.5 position; compact survey log; Markdown download verified from its actual file contents. Account/ledger sync is a later protocol decision, not part of this local-first closeout.
3. **Split atlas summary data from L1 interiors** — ✅ current working tree: generated 128-entry L0 projection + drift gate; direct package subpath imports; generated-island screen lazy-loaded; stale-server interior fallback dynamically loaded. Production main 811.84 kB / 263.87 kB gzip (−69.4% / −72.3% from baseline), with the deep archive absent from initial preload.
4. **Accessibility + semantic-control pass** — **next.** Replace L2 clickable `div`/`span`, add keyboard/focus states, raise small touch targets, and correct the known ochre/pale-ink contrast failures.
5. **Structured field board, not a blank canvas** — when the deferred whiteboard resumes, position it around route observations, question clusters, evidence cards, and island/current links so it extends the notebook and still folds into the existing Yjs/ledger artifact path.
6. **Relations lens** (§3.10), then scale honesty / richness continuation / ferryman economy and interop in that order. Build on the shipped LOD-gated air-route form; do not restore the always-on cluttered sea layer.

- **Relations rework — a better form, not "render more sea-plane"** (§3.10): the always-on sea-plane relations are now hidden, so the goal is no longer to add more always-on flow visuals but to re-form the relation layer as an **on-demand relations lens** (toggle / focus-only / LOD-gated). Only then decide which of whirlpool (data already projected), strait/isthmus (relation maturity), confluence/estuary (cross-domain synthesis), trade wind, and archipelago auto-clustering earn a place — graduating survivors from design-system cards to live components via the design-eng-loop.
- **Canvas scaling** — answers "one canvas can't hold many islands" (§3.11). Per `depth-plan-v1.md` §3: (a) deterministic layout / de-overlap pass (started in this change); (b) zoom + pan camera; (c) LOD cartographic generalization with variance-select culling (far = coastline + lighthouses + outlier glow; mid = named clusters; near = full islands); (d) My Harbor 我的港湾 default entry + 雾化 fog filter; (e) swap L0 to the PixiJS engine (already built behind `packages/renderer/./pixi`, contract + thumbnails done) beyond ~100 islands; hybrid cluster layout (§8 open decision 5).
- xfrontier nine-dimension score → island form params; outlier glow from real variance.
- Bridges + ferryman routing economy (biddable open problems over MCP).
- ORCID, Typst/PDF + Zenodo DOI export, OKH/RO-Crate search, Merkle anchoring (OpenTimestamps).

### Phase D — P4 federation
- ActivityPub follow, Open Night 开放之夜 + badge, multi-agent division of labor, optional on-chain notarization.

## 5 · Open decisions (need a human call; none block Phase A)
1. Final name (问题群岛 / 格致群岛 / …) — affects domain, OAuth app, repo name.
2. Night-layer default visibility norm (island-members vs public).
3. Default licenses (suggest CC-BY-4.0 + MIT).
4. `opp/0.3`: native actions for `attach_data`/`attach_hardware`/`create_driftwood`? (Breaking; needs a change note.)
5. Agent identity registry: keep `github:` prefixes vs `did:` for AI residents.
6. Ratify `depth-plan-v2.md` invariants 14–16 into `architecture.md` once the sea-plane proves out (in its reworked on-demand form, §3.10) — and, once adopted, fold `depth-plan-v1.md` (invariants 10–13 + 17) and the nested-vertical-world contract in the same way (§3.12).
7. **Who owns island altitude?** The atlas contract makes altitude pure cartography (`altitudeZ` = folded place-plane position, never meaning); `depth-plan-v1.md` §1 wanted sky/sea/seabed = epistemic groundedness. Options: (a) altitude stays cartographic, groundedness moves to a different channel (sea *depth* is already abstractness per v2 — the symmetric answer); (b) altitude becomes groundedness and cartography re-folds; (c) split by LOD. Must be decided before v1 ratification.
8. ~~Mobile write path~~ — **resolved 2026-07-12** (`DECISIONS.md` #10): the mobile shell stays a read-only browse companion, frozen — no Pixi atlas, L1, or ledger writes on mobile; breakpoint raised 640 → 900 so the desktop stage only renders where it is legible. Reopen only if real mobile-writing demand appears (`ui-interaction-pass-2026-07-11.md` debt 2).

## 6 · Session log
- **Slice 1** — monorepo, protocol, domain, renderer, assets, server, web, OAuth (`6915d23…431601c`).
- **Slice 2** — curated xfrontier atlas + scene generator + Track B founding (`0e7c052…aebf3c6`).
- **Slice 3** — v2 「海即数据」 sea-plane + `.design-eng-loop` co-evolution (`7c97979…8de5306`).
- **Slice 4** — Phase B closed: CrossRef scout resident → ledger night replay → transplant-through-dock → evidence enforcement + steles → webhook → §8 chain accepted live (`90a7217…3d96e40`).
- **Slice 5** — richness campaign: `depth-plan-v1.md` reconstructed (`317f8ab`), L1 layered Pixi scene engine + night replay (`docs/scene-upgrade/`).
- **Slice 6** — world-map campaign W1–W6 (`5f9307d…17f5aa2`): default Pixi L0, 4-layer world, k-means++ at 700; exploration + UI flagship passes; vertical archipelago (`2948013`); V1 nested vertical world landed (`548fa26`).
- **Slice 7** — atlas scale + presentation (2026-07-11/12): frontier atlas tripled 26→78 islands with grounded per-island interiors (`50eca00`); flagship island interiors — every station opens with distinct content, buildings reliably clickable (`43d5944`, `532930d`, `660ce73`, `1d7a3b1`); atlas presentation polish — comfortable-at-every-tier (`9b13426`), then world-tier legibility (domain-label paper-halo + push-out so region chips no longer slice the names), far-tier route restraint, cluster-card domain ink, `ATLAS_Y_SPREAD` vertical framing, and eased wheel-zoom (`9a50eb4`).
- **Slice 8** — continuous world exploration + local-first notebook (current 2026-07-16 working tree): atlas expands to 128 islands; L0.5 adds continuous craft movement, question compass/course, altitude strata, current sampling, survey notes, L1 docking, and berth-preserving return. Closeout adds the first repository-level full-stack Playwright smoke, fixes post-survey residual drift, persists the notebook across reloads, exports the exact record as Markdown, and closes the StrictMode/Pixi teardown race found by the stronger browser trace.
- **Slice 9** — Atlas/L1 data boundary (current 2026-07-16 working tree): generated L0-only Atlas summaries, package subpath exports, lazy generated-island UI, and a conditional deep-interior fallback reduce the main production bundle 69.4% raw / 72.3% gzip while preserving all 48 rich flagship interiors. Next: the focused L2 accessibility + semantic-control pass.
